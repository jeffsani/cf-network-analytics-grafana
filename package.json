{
  "name": "grafana-network-analytics",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Network Analytics Grafana Dashboard on Worker Containers",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types"
  },
  "dependencies": {
    "@cloudflare/containers": "^0.2.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250417.0",
    "typescript": "^5.7.0",
    "wrangler": "^4.83.0"
  }
}
